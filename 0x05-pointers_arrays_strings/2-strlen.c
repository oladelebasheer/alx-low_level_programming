#include "main.h"

/**
 * _strlen - Return the length of a string
 * @s: string
 * Return: return length as an integer
 */

int _strlen(char *s)
{
	int len = 0;
	while (*(s _ len) != '\0')

	return (len);
}
